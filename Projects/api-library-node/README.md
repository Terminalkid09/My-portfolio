# Books REST API with Node.js 📚

Simple REST API to manage a book library.

## Endpoints
- GET `/api/books` → all books
- GET `/api/books/:id` → book by ID
- POST `/api/books` → add book
- PUT `/api/books/:id` → update book
- DELETE `/api/books/:id` → delete book
## Screenshot
![GET all books](first.png)
![POST add book](afterPOST.png)

## How to run
bash
npm install
npm start

Project di **@Terminalkid09**